// SDL3/SDL_time.h - stub. Source/System/Menu.c includes this directly
// (rather than just <SDL3/SDL.h>); everything it needs (SDL_Time/
// SDL_DateTime/SDL_TimeToDateTime/SDL_GetCurrentTime) is already declared
// in SDL.h, so this just forwards there.
#pragma once
#include "SDL.h"
