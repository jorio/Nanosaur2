// SaverBridging.h - bridging header for the screen-saver build of the
// engine module (ports/Darwin). The game's own bridging header plus the
// saver shim declarations (shim.c) that replace SDL at runtime.

#pragma once

#include "../../../Source/Headers/Nanosaur2-Bridging-Header.h"
#include "saver_shim.h"
